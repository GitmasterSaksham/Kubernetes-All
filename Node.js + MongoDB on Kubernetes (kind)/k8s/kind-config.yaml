kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30030  # Kubernetes NodePort for your app
        hostPort: 3000       # Expose it on localhost
